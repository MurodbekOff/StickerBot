# Copy this file to .env and fill in real values for LOCAL development only.
# .env is git-ignored -- never commit real secrets.
# On hosting platforms, set these as environment variables/secrets in their
# dashboard instead of uploading a .env file.

BOT_TOKEN=123456:your-real-bot-token-from-botfather
BOT_USERNAME=your_bot_username
